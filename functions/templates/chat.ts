import type { Template } from '../types';

const template: Template = {
  type: 'text',
  description: "通用聊天助手",
  systemPrompt: "你是一个友好、专业的AI助手。你会以简洁、准确的方式回答问题，并在必要时提供详细解释。",
};

export default template;
